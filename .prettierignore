dist
node_modules
.luxe
package-lock.json
CHANGELOG.md

# The internal design skill. `tokens.css` and `adherence.json` in it are written
# by scripts/build-design-skill.js, and `npm run check` fails when they differ
# from what that script produces - so Prettier reformatting them would deadlock
# the check chain against its own generator. The skill has its own formatting
# gate instead: scripts/check-design-adherence.js.
.agents

# The kitchen-sink artifact is a verified Luxe output kept byte-for-byte as the
# agent wrote it, so it stays a faithful sample of what Luxe actually produces.
# Reformatting it would edit the thing under test.
examples/kitchen-sink.html
